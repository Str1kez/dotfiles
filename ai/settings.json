{
  "alwaysThinkingEnabled": true,
  "autoUpdatesChannel": "stable",
  "theme": "dark-ansi",
  "editorMode": "vim",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(jq -r '.tool_input.file_path') && if [[ \"$FILE\" == *.py ]]; then uv run ruff check --fix \"$FILE\"; uv run ruff format \"$FILE\"; fi"
          }
        ]
      }
    ]
  }
}
